//! https://leetcode.cn/problems/jump-game-ii/

struct Solution();

#[allow(dead_code)]
impl Solution {
    pub fn jump(nums: Vec<i32>) -> i32 {
        //
    }

    pub fn is_conn(x: usize, y: usize, c: u8) -> bool {
        // if c == 0, 
        // loop from end to start, select one, call is_conn(x, y, 0)
    }
}
